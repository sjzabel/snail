language: python
python:
  - "3.4"
# command to install dependencies
install:
  - "pip install ."
  - "pip install -r requirements.txt"
  - "pip install -r tests/requirements-for-testing.txt"
# command to run tests
script: nose2
